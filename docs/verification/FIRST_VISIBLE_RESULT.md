# 첫 화면 검토

2026-09-15 · WAITING_FOR_REVIEW

## 상태와 미리보기

로컬 개발 서버: http://127.0.0.1:5190
- 공개 목록: /
- FIRST SEAT 공모: /first-seat
- FIRST SEAT 투표: /first-seat?stage=voting
- 통합 Admin: /admin

위 주소는 검토용 로컬 실행이며 회사 도메인 배포 완료가 아닙니다. 실제 제품에서는 query로 단계 접근을 허용하지 않고 서버 공개 단계로 제한합니다.

## 구현한 대표 범위

- 흑백 공개 이벤트 목록·공모·투표·결과 예시 화면.
- Radix Dialog/Tabs 기반 Admin 목록, 템플릿 선택, 새 이벤트 구성, 모듈 추가/삭제/재정렬/문구 수정.
- 투표 연락처·이메일·인스타그램 입력 및 운영자 중복 허용/제한 설정 화면.
- 공통 토큰과 컴포넌트 라이브러리 샘플.
- 검토용 비개인 구성만 브라우저 localStorage 저장. 참여자 입력/개인정보는 저장하지 않음.

## 검증

- `npm run check:preview`: 통과.
- `npm run build:preview`: 통과.
- `node tests/preview/browser.mjs`: Chrome, 공개 목록/공모/투표/Admin을 1440·390·320px에서 검증, 가로 넘침 없음.
- 문구 편집 후 미리보기 갱신/새로고침 후 복원, 모듈 추가·이동·삭제, 투표 설정, 새 이벤트 생성 검증.
- 신규 검토 화면의 브라우저 pageerror 없음.
- 결과: artifacts/preview/results.json. 스크린샷은 같은 폴더. 검수 산출물은 Git에서 제외.

## 아직 구현하지 않은 범위

신규 D1 생성/새 다중 이벤트 migration, 실제 접수/심사/투표/동의 저장, 정보 기반 중복 차단, Google OAuth/서버 인증, 이미지 업로드, 기능 연결 공개 검사, 실제 배포/원격 push.

현재 앱은 apps/preview의 별도 검토 진입점으로 실행합니다. 기존 FIRST SEAT 코드가 재사용 기반으로 남아 있으나 새 플랫폼 기능이 완성되었다는 뜻이 아닙니다. Admin의 공개 구성 표시는 예시 상태이며 게시를 실행하지 않습니다. PC 탭은 넓은 패널의 배치 검토용이고 정확한 1440px iframe 미리보기는 이후 연결합니다. 실제 공개 화면 자체는 1440px 브라우저에서 검증했습니다.

설치 시 기존 의존성에서 npm audit 33건(critical 2건 포함)이 보고되었습니다. 아직 개별 영향 분류·수정 전이며 배포 검증 단계에서 해결해야 합니다. 최초 화면 검토 통과는 제품 보안/배포 준비 완료를 뜻하지 않습니다.

## 검토 후 다음 단계

공개 흑백 디자인과 Admin의 목록/모듈 편집 방식에 대한 사용자 검토 후, 기존에 승인된 범위의 실제 데이터 기능을 연결합니다. `/admin`과 회사 계정 전체 자동 등록은 이미 확정되어 재승인 대상이 아닙니다.

## 첫 피드백 수정 결과

공개 목록 삭제, 루트 빈 404, FIRST SEAT 검정 테마/지정 카피/폰트, 레이블·구분선 제거, 푸터 2개 항목, Admin 기능 중심 문구와 라이브러리 메뉴 제거 완료.
공모/투표/Admin을 1440·390·320px에서 재검증. 루트 HTTP 404·빈 body, 공개 페이지 내 루트 링크 없음, 보조 레이블 없음, 검정 테마·기본 자간을 검사. 편집·저장 복원·모듈 순서·이벤트 생성 회귀 통과.
개인정보 처리방침/문의는 검토용 안내이며 실운영 연결이 아님. 실제 데이터/OAuth/배포 완료 여부는 이전과 동일하게 미구현.

## 2026-09-15 추가 타이포그래피 피드백 반영

- 공모·투표·결과 공통 헤더: SEOUL ARENA + 이벤트 제목(FIRST SEAT), Archivo Bold와 기본 자간.
- 본문은 영문·숫자를 포함해 Pretendard로 통일. 소개 첫 문장은 본문 크기·흰색, 설명은 회색.
- 공모 폼의 제목/설명 제거, 제출 버튼은 “문구 보내기”.
- 편집기에 모든 단계 공통 이벤트 제목, 제목 크기 H1~H4/본문, 제목·본문 색상 기본(회색)/강조(흰색) 선택 추가. 임의 CSS 대신 공통 토큰을 사용.
- 결과 주요 문구 H1, 후속 안내 H4로 시각 위계 분리. HTML 문서의 페이지 h1은 공통 이벤트 제목으로 유지.
- 타입 검사/프로덕션 빌드 통과. Chrome에서 공모·투표·결과·Admin 1440/390/320px 가로 넘침 없음. 편집 저장/복원, 모듈 추가·순서·삭제, 크기·색상 선택 및 결과 위계, 투표 설정, 템플릿 생성 검사 통과.
- 화면 검토용 구현이며 실제 접수·로그인·데이터 처리·push·배포는 아직 진행하지 않음. WAITING_FOR_REVIEW 유지.

## 2026-09-15 저장/공개 오류 구분 수정

원격 신규 D1의 FIRST SEAT는 revision 4, draft 상태이며 event.draft_saved 기록 3건을 확인했다. 개인정보 처리방침 길이는 0으로 공개 조건을 충족하지 않는다. 실제 사용자 콘텐츠·참여 데이터는 수정하지 않았다. 관리자 Worker가 모든 예외를 저장 실패로 바꾸던 처리를 허용된 안내 문구만 전달하도록 수정했다. 운영 화면에 초안 저장 상태와 처리방침 작성 필요 안내를 추가했다. 타입 검사·빌드·오류 안내 테스트 2개 통과. 구조 개편안 승인을 대신하는 변경은 아니다.

## 단계 전환 준비 조건 보완

기존 FIRST SEAT Dashboard의 blockedReasons 기반 전환 흐름을 확인했다. 신규 transition-preview가 공개 여부·페이지 구성·후보 확정·결과 선정·단계별 동의문 준비 상태를 반환하며 화면 버튼과 서버 실행에서 동일한 준비 결과를 사용하도록 수정했다. 확인창에서도 canTransition이 false이면 실행할 수 없다. revision/activityRevision의 최종 동시 변경 검증은 유지한다. 로컬 가상 이벤트에서 후보 미확정의 전환 API 409·단계 보존·확정 후 준비 완료·결과 미선정 차단·전체 참여 흐름을 확인했다. 운영 이벤트 단계나 참여 데이터는 변경하지 않았다.

## 공개 준비 및 이벤트 링크

원격 FIRST SEAT는 revision 4 초안, 처리방침 미입력, stage_policies 0건을 확인했다. 공개 준비 화면에 처리방침·문의·현재 단계 동의문 입력을 연결하고 누락 시 공개 버튼을 비활성화했다. 조건 입력 후 설정·동의 버전을 저장한 뒤 공개한다. 로컬 가상 이벤트로 누락 차단과 실제 published 상태까지 검증(tests/platform/publish-ui.mjs). 원격 FIRST SEAT 원문 작성·공개는 하지 않았다.

목록과 편집 화면에 전체 이벤트 링크, 새 창 열기와 복사를 추가했다. 새 이벤트 생성 시 주소를 입력하고 초안에서 수정할 수 있으며 공개된 주소는 고정한다. 기존 모든 이벤트에서 first-seat를 열던 버튼을 실제 이벤트 slug로 수정했다. 운영 저장 후 편집으로 돌아갈 때 상태가 갱신되도록 연결했다.

## 이벤트 주소 영역 간격 수정

주소 링크·라벨·입력·도움말·복사 버튼을 전용 flex 영역으로 묶고 간격과 줄바꿈을 명시했다. 로컬 Chrome 1440px/390px에서 요소 겹침과 가로 넘침 없음 확인, 모바일 캡처 육안 확인, 타입 검사·빌드 통과. 데이터와 공개 주소 값은 변경하지 않았다.

## 관리자 시스템 색상·가독성

packages/ui/src/AdminStatus.tsx 및 admin-status.css에 백오피스 전용 의미별 토큰·공지·심사 배지를 추가했다. 성공/승인 녹색, 오류/반려 빨강, 주의/검토대기 노랑, 안내/후보 파랑. 상태명과 역할을 함께 제공한다. 운영 부모 메뉴와 현재 하위 메뉴가 동시에 활성 배경을 갖던 문제를 해결했다. PC 1440px·모바일 390px에서 활성 메뉴 1개와 가로 넘침 없음 확인. 공개 페이지 색상은 변경하지 않았다. 공개 설정 제목·본문·입력·경고 간격도 정리했다.

## 공개 푸터 정렬

미리보기 전용 선택자가 실제 문의 링크에도 적용되던 CSS 범위를 수정했다. 처리방침 버튼과 문의 링크를 공통 14px 폰트·행간·굵기·밑줄·44px 클릭 영역으로 통일하고 한 줄 중앙 정렬했다. 로컬 1440px/390px에서 버튼+링크 조합의 동일 글꼴·상단 위치·행 중앙을 확인하고 모바일 캡처를 검토했다. 빌드 통과.

## 동의 항목 편집과 실제 운영 연결

동의 모듈에서 체크박스 문구·자세히 보기 원문·추가 항목 편집 지원. 기본 개인정보/응모작 항목은 유지하고 추가 항목은 제외 가능. 현재 항목은 필수 동의로 저장한다. 초안 설정을 공개할 때 동의문+체크 문구를 버전별 JSON 원문으로 기록하고 기존 버전은 변경하지 않는다. 과거 평문 동의문도 읽으며 기존 설정은 에디터에 불러온다. 공개 시 활성 동의 연결을 갱신하고 과거 참여 이력은 보존한다.

입력 모듈 표시명은 ‘입력창’으로 변경. 공개페이지 테스트 링크 및 실제 로컬 API 기본 연결. 에디터 미리보기임을 운영 서비스 준비 상태와 구분하도록 문구 수정. 실제 운영 키가 필요한 Turnstile은 미등록으로 담당자 연결 대기.

검증: 단위 109개, 타입 검사·빌드 통과. 로컬 추가 필수 동의 누락 차단·접수·심사·후보확정·투표·결과·범위 초기화 통합 검증 통과. consent-editor 브라우저에서 항목 추가·문구/원문 수정·저장·자세히 보기 반영 검증. 실제 운영 데이터 삭제는 수행하지 않음.

## 운영 연결 최종 확인 중

Turnstile 신규 생성·키 등록·공개 Worker 배포 완료. 실 도메인의 자동 Chrome 검증은 600010으로 거절되어 실제 일반 브라우저 확인을 사용자에게 요청했다. 운영 참여 성공으로 보고하지 않는다. 별도 검증 이벤트만 생성했고 FIRST SEAT 접수/일정/참여 데이터는 변경하지 않았다. 로컬 캠페인 전체 흐름·동의 추가·초기화 검증, 단위 109개 및 SQL 분리 6개 통과. 런타임 의존성 npm audit 0개. 최신 Chrome 전화번호 pattern의 하이픈 이스케이프 수정. Turnstile 중복 자동 실행을 피하도록 execution=execute 설정, 스크립트 실패 재시도 가능하게 수정.
